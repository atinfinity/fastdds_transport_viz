<?xml version="1.0" encoding="UTF-8" ?>
<!-- QoS for the statistics DataWriters that FASTDDS_STATISTICS enables in observed nodes.

     Fast DDS 2.14 applies the default resource limit of 10 instances to those
     writers. RTPS_SENT is keyed by destination locator, so a participant that
     talks to more than 10 locators stops reporting the extra ones and
     transport_viz - -stats shows "stats-writer-instance-limit-suspected".
     This profile lifts the limit (0 = unlimited) while keeping the QoS the
     statistics module requires. Profile names must match the aliases passed
     in FASTDDS_STATISTICS. Use with:
       export FASTRTPS_DEFAULT_PROFILES_FILE=$(ros2 pkg prefix fastdds_transport_viz)/share/fastdds_transport_viz/config/statistics.xml
       export FASTDDS_STATISTICS="RTPS_SENT_TOPIC;HISTORY_LATENCY_TOPIC;PHYSICAL_DATA_TOPIC" -->
<dds xmlns="http://www.eprosima.com/XMLSchemas/fastRTPS_Profiles">
  <profiles>
    <data_writer profile_name="RTPS_SENT_TOPIC">
      <topic>
        <historyQos><kind>KEEP_LAST</kind><depth>10</depth></historyQos>
        <resourceLimitsQos>
          <max_samples>0</max_samples>
          <max_instances>0</max_instances>
          <max_samples_per_instance>0</max_samples_per_instance>
          <allocated_samples>100</allocated_samples>
        </resourceLimitsQos>
      </topic>
      <qos>
        <durability><kind>TRANSIENT_LOCAL</kind></durability>
        <reliability><kind>RELIABLE</kind></reliability>
        <publishMode><kind>ASYNCHRONOUS</kind></publishMode>
      </qos>
    </data_writer>
    <data_writer profile_name="HISTORY_LATENCY_TOPIC">
      <topic>
        <historyQos><kind>KEEP_LAST</kind><depth>10</depth></historyQos>
        <resourceLimitsQos>
          <max_samples>0</max_samples>
          <max_instances>0</max_instances>
          <max_samples_per_instance>0</max_samples_per_instance>
          <allocated_samples>100</allocated_samples>
        </resourceLimitsQos>
      </topic>
      <qos>
        <durability><kind>TRANSIENT_LOCAL</kind></durability>
        <reliability><kind>RELIABLE</kind></reliability>
        <publishMode><kind>ASYNCHRONOUS</kind></publishMode>
      </qos>
    </data_writer>
  </profiles>
</dds>
